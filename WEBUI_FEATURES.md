# 📊 股票分析系统 - 功能更新说明

## 🎉 新增功能概览

本次更新为项目提供了完整的 WebUI 自选股管理功能和报告展示能力。

### 核心新增功能

#### 1️⃣ **自选股数据库存储**
- ✅ 数据独立存储在数据库中（不依赖 .env）
- ✅ 支持自动创建 Watchlist 表
- ✅ 支持按市场分组（A股、港股、美股）
- ✅ 支持排序和批量管理

#### 2️⃣ **现代化 WebUI 界面**
- ✅ 全新设计的响应式界面
- ✅ 自选股卡片式展示
- ✅ 按市场分组展示（🇨🇳 A股、🇭🇰 港股、🇺🇸 美股）
- ✅ 响应式设计（适配手机、平板、桌面）

#### 3️⃣ **自选股管理功能**
- ✅ 在页面上直接添加自选股（支持输入代码、名称、市场类型）
- ✅ 一键删除自选股
- ✅ 快速查看股票分析历史
- ✅ 即时分析功能（点击分析按钮）

#### 4️⃣ **分析报告展示**
- ✅ 查看股票历史分析报告
- ✅ 显示关键指标（买卖建议、情绪评分）
- ✅ 完整摘要展示
- ✅ 按时间排序的报告列表

---

## 🚀 使用指南

### 启动 WebUI

```bash
cd /workspaces/daily_stock_analysis

# 方式一：使用启动脚本（推荐）
./start-webui.sh

# 方式二：直接运行 Python
python3 main.py --webui-only

# 方式三：使用 webui.py
python3 webui.py
```

### 访问地址

```
http://127.0.0.1:8000
```

### 主要功能页面

#### 📌 自选股管理
1. **添加自选股**
   - 输入股票代码（支持 A股/港股/美股）
   - 可选输入股票名称
   - 选择市场类型
   - 点击"✚ 添加"按钮

2. **管理自选股**
   - 自选股按市场分组展示
   - 每个股票卡片包含：
     - 股票代码和名称
     - 📊 分析按钮（实时分析）
     - 📜 历史按钮（查看历史报告）
     - × 移除按钮（删除自选股）

#### 📜 查看分析历史
1. 点击股票卡片中的"📜 历史"按钮
2. 自动加载最近 5 条分析报告
3. 显示内容包括：
   - 分析时间
   - 操作建议（买/卖/持有）
   - 分析摘要
   - 情绪评分

#### ⚙️ 系统设置
- 数据库自动管理
- 自平台配置管理
- API 接口支持

---

## 🔌 API 接口

### 自选股管理 API

#### 添加自选股
```
GET /watchlist/add?code=600519&name=贵州茅台&market=CN
```

**参数：**
- `code` (必填) - 股票代码
- `name` (可选) - 股票名称
- `market` (可选，默认 CN) - 市场类型（CN/HK/US）

**返回：**
```json
{
  "success": true,
  "code": "600519",
  "name": "贵州茅台",
  "market": "CN"
}
```

#### 删除自选股
```
GET /watchlist/remove?code=600519
```

**返回：**
```json
{
  "success": true
}
```

#### 获取自选股列表
```
GET /watchlist/list
```

**返回（按市场分组）：**
```json
{
  "success": true,
  "data": {
    "CN": [
      {"code": "600519", "name": "贵州茅台", "market": "CN", ...},
      ...
    ],
    "HK": [...],
    "US": [...]
  }
}
```

#### 获取单市场自选股
```
GET /watchlist/list?market=CN
```

**返回：**
```json
{
  "success": true,
  "data": [
    {"code": "600519", "name": "贵州茅台", ...}
  ]
}
```

#### 获取股票分析历史
```
GET /stock/analysis?code=600519&limit=10
```

**参数：**
- `code` (必填) - 股票代码
- `limit` (可选，默认 10) - 获取数量

**返回：**
```json
{
  "success": true,
  "code": "600519",
  "data": [
    {
      "code": "600519",
      "name": "贵州茅台",
      "operation_advice": "建议关注",
      "sentiment_score": 75,
      "analysis_summary": "...",
      "created_at": "2026-02-06T10:30:00"
    }
  ]
}
```

---

## 📊 数据库结构

### Watchlist 表
```python
class Watchlist:
    id: int              # 主键
    code: str            # 股票代码（唯一）
    name: str            # 股票名称
    market: str          # 市场类型（CN/HK/US）
    order_key: int       # 排序权重
    remarks: str         # 备注信息
    created_at: datetime # 创建时间
    updated_at: datetime # 更新时间
```

---

## 🔄 工作流程

```
┌─────────────────┐
│ 打开 WebUI      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────────┐
│ 查看自选股      │◄─────┤ 按市场分组展示   │
└────────┬────────┘      └──────────────────┘
         │
    ┌────┴────┬─────────┬─────────┐
    │          │         │         │
    ▼          ▼         ▼         ▼
  添加      分析      历史      删除
  股票      股票      报告      股票
    │          │         │         │
    └────┬─────┴────┬────┴────┬────┘
         │          │         │
         ▼          ▼         ▼
      数据库      API      页面刷新
```

---

## 💡 功能亮点

### 市场分类
- **A股** 🇨🇳：支持沪深两市（600xxx/601xxx/602xxx/000xxx/002xxx/300xxx）
- **港股** 🇭🇰：支持香港上市公司（HKxxxxx 格式）
- **美股** 🇺🇸：支持美国上市公司（AAPL/BRK.B 等）

### 数据持久化
- 所有自选股保存在本地数据库
- 支持长期跟踪
- 支持分析历史回溯

### 即插即用
- 无需修改 .env 配置
- 纯 Web 界面操作
- 响应式设计，支持多终端

---

## ⚙️ 配置指南

### WebUI 环境变量

在 `.env` 文件中配置：

```bash
# WebUI 监听地址
WEBUI_HOST=127.0.0.1

# WebUI 监听端口
WEBUI_PORT=8000

# 是否启用 WebUI
WEBUI_ENABLED=true
```

### 数据库路径

默认使用：
```bash
DATABASE_PATH=./data/stock_analysis.db
```

---

## 🐛 常见问题

### Q: 添加了自选股但页面没有显示？
**A:** 
1. 检查浏览器控制台是否有错误
2. 刷新页面重新加载数据
3. 检查数据库文件是否存在：`./data/stock_analysis.db`

### Q: 如何批量导入自选股？
**A:** 目前支持逐个添加，可以通过 API 或脚本批量调用：
```python
from web.services import get_watchlist_service
svc = get_watchlist_service()
svc.add_stock('600519', '贵州茅台', 'CN')
svc.add_stock('000001', '平安银行', 'CN')
```

### Q: 如何导出自选股列表？
**A:** 通过 API 获取并保存：
```bash
curl http://127.0.0.1:8000/watchlist/list | python3 -m json.tool > watchlist.json
```

### Q: 自选股删除后能恢复吗？
**A:** 删除后无法恢复，请谨慎操作。

---

## 🔐 安全性说明

- WebUI 仅在本地 127.0.0.1 监听（可通过 WEBUI_HOST 配置）
- 所有数据存储在本地数据库
- 无远程连接或上传
- 建议在专网环境下使用

---

## 📈 后续计划

- [ ] 实时数据刷新
- [ ] Watchlist 导入/导出
- [ ] 分析报告详情展示
- [ ] 预警提醒设置
- [ ] 性能对标功能
- [ ] 高级图表展示

---

## 🙏 支持与反馈

- 📖 项目文档：[docs/full-guide.md](ocs/full-guide.md)
- 🐛 问题反馈：通过 GitHub Issues 提交
- 💬 讨论交流：GitHub Discussions

---

**更新日期**：2026-02-06  
**版本**：v2.0 - WebUI 自选股管理功能
